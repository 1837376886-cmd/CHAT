<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.chat.mapper.CsSessionMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.chat.domain.entity.CsSession">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="visitor_id" property="visitorId" jdbcType="BIGINT"/>
        <result column="cs_user_id" property="csUserId" jdbcType="BIGINT"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="start_time" property="startTime" jdbcType="TIMESTAMP"/>
        <result column="cs_unread_count" property="csUnreadCount" jdbcType="INTEGER"/>
        <result column="end_time" property="endTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectActiveSessionsByCsUserId" resultMap="BaseResultMap">
        SELECT * FROM cs_session
        WHERE cs_user_id = #{csUserId}
          AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectActiveSessionByVisitorId" resultMap="BaseResultMap">
        SELECT * FROM cs_session
        WHERE visitor_id = #{visitorId}
          AND status = 1
        LIMIT 1
    </select>

    <select id="selectSessionsByVisitorId" resultMap="BaseResultMap">
        SELECT * FROM cs_session
        WHERE visitor_id = #{visitorId}
        ORDER BY create_time DESC
    </select>

    <select id="countActiveSessionsByCsUserId" resultType="int">
        SELECT COUNT(*) FROM cs_session
        WHERE cs_user_id = #{csUserId}
          AND status = 1
    </select>

    <update id="incrementUnreadCount">
        UPDATE cs_session
        SET cs_unread_count = cs_unread_count + 1
        WHERE id = #{sessionId}
    </update>

</mapper>
