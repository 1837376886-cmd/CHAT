# 专属客服功能设计方案

> 状态：v3.1（待确认问题已决策，可直接实施）
> 日期：2026-05-06
> 关联文档：`客服系统独立化改造方案.md`
> 预计工期：**1.7 周**

---

## 一、需求背景

业务方希望在现有"在线客服"系统基础上引入**专属客服（一对一强绑定）**能力：

1. 来自外部业务系统的**已登录用户**（即另一套若依的 `sys_user`，客服系统通过从库读取），其 `sys_user` 表上携带一个 `bound_cs_user_id` 字段标识专属客服。
2. 该用户咨询时**只与专属客服对话**，不走默认分配。
3. 专属客服**不在线**时，访客侧文案提示「已发送短信通知您的专属客服，请稍候」，并将会话挂起等待客服上线（后续接入真实短信/邮件通知）。
4. 已登录用户可**多端同时打开**客服窗口（APP / 小程序 / PC），各端**独立会话**，消息**互不广播**，客服分别回复。
5. 每条消息携带**来源标识**（deviceType），客服可看到用户从哪个端发来。
6. **必须兼容**现有匿名访客流程（无外部身份的访客继续走原"粘性 + 默认最少接待数"算法）。

---

## 二、决策结论（已锁定）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 外部用户绑定关系存放 | **从库 `sys_user.bound_cs_user_id`**（外部系统侧改 schema，客服系统只读） |
| 2 | 身份识别 | **前端直接传 `externalUserId`**：`/cs/connect` 携带 `externalUserId`，后端查从库 `sys_user` 确认该 userId 存在即可 |
| 3 | 客服身份标识位维护 | **复用现有 `cs_config` 表**：`user_id` 存在 + `status=1` 即代表"启用中的客服"；从库 sys_user 不动 |
| 4 | 多端会话 | **各自独立**：每次打开新建 `cs_session`，多端消息互不广播，客服分别回复每个 session |
| 5 | 登录用户接待名额 | **不占 `max_sessions`**：接待名额仅约束匿名访客，登录用户会话不计入 activeCount；`max_sessions` 配置项加说明"仅约束匿名访客" |
| 6 | 专属客服离线 | **严格专属 + 排队等待**，UI 文案"已发送短信通知"（**留 TODO**：后续接入真实通知渠道后再调整文案），复用 `cs:pending` 挂起，客服上线自动接管 |
| 7 | 专属客服失效（为空/取消客服身份/禁用/删除）| **拒绝服务**，提示"请联系您的业务联系人重新分配专属客服"；**本期不降级为匿名流程** |
| 8 | 通知触发实现 | **仅留入口**：定义 `NotifyService` 接口 + 触发点，实现类先用日志打印；后续讨论确定通知方式（短信/邮件/企微）后再实装 |
| 9 | 专属会话转接 | **完全禁止**（终身绑定，除非客服离职或管理员介入换绑） |
| 10 | 前端工作台设计 | **好友式分组**：左侧分"📌 我的客户"（登录用户按用户聚合）和"💬 临时会话"（匿名访客）两组；客服回复广播给该用户所有在线端；**离线超过 3 天的用户折叠到"历史客户"子分组**（阈值可在 `cs_config.offline_fold_days` 动态配置） |
| 11 | 历史消息 | **客服侧和用户侧均提供**：后端提供跨 session 聚合查询（按 `cs_user_id + external_user_id`），用户侧打开客服窗口即加载全部历史消息（分页上滑） |

---

## 三、现状梳理

| 维度 | 现状 | 本次改造影响 |
|---|---|---|
| 访客身份 | 仅 `visitor_token`（匿名） | `chat_visitor` 加 `external_user_id` 关联从库 sys_user.id |
| 分配总入口 | `CustomerServiceAllocationService.allocate()` | 开头加专属判定短路分支 |
| 客服身份 | `sys_user.is_customer_service`（本地写入） | 改造后 sys_user 在从库不可写，**复用本地 `cs_config.status` 作为身份标识** |
| 离线挂起 | `cs:pending:{csUserId}` + `processPendingOnCsOnline()` | 完全复用，零改造 |
| 等待结果 | `AllocationResult.waitingForLastCs` 是空壳 | 本次激活真值 |
| 转接 | `/cs/transfer` | 增加"专属会话禁止转接"校验 |
| 通知能力 | 无 | 新增 `NotifyService` 接口 + 空实现 |
| 多端推送 | 单端 `visitorToken → Channel` | 新增 `externalUserId → Set<Channel>` 映射，**客服回复时广播到该用户所有端** |

---

## 四、数据库变更

### 4.1 从库（外部系统侧改）

> ⚠️ 由外部业务系统的 DBA / 后端团队执行，客服系统只读。

```sql
ALTER TABLE sys_user
  ADD COLUMN bound_cs_user_id bigint DEFAULT NULL
    COMMENT '专属客服 sys_user.id；NULL=未绑定；与 cs_config.user_id 对应（status=1 才有效）'
    AFTER status,
  ADD KEY idx_bound_cs (bound_cs_user_id);
```

### 4.2 客服身份：复用 `cs_config` 表（无需新表）

`cs_config` 表已有结构：

```sql
CREATE TABLE `cs_config` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '客服用户ID',
  `max_sessions` int DEFAULT 5 COMMENT '最大同时接待数（仅约束匿名访客）',
  `auto_reply` varchar(500),
  `status` tinyint DEFAULT 1 COMMENT '0-禁用，1-启用',
  `update_time` datetime,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`)
);
```

**身份语义约定**（本次新增的业务规则，不改 schema）：

> `cs_config` 中 **`user_id` 存在**且 **`status=1`** ⇔ 该 sys_user 是"启用中的客服"。

**迁移**：一次性 SQL，把原本 `is_customer_service=1` 但 `cs_config` 中尚未存在记录的用户补齐：
```sql
INSERT INTO cs_config (user_id, max_sessions, status)
SELECT u.user_id, 5, 1
FROM sys_user u
LEFT JOIN cs_config c ON c.user_id = u.user_id
WHERE u.is_customer_service = 1 AND c.id IS NULL;
```

**后续代码改动**：
- `CustomerServiceController.setStaff` 改为操作 `cs_config`：设置客服 = `INSERT ... ON DUPLICATE KEY UPDATE status=1`；取消客服 = `UPDATE cs_config SET status=0 WHERE user_id=?`（保留配置数据，只切状态）。
- `CustomerServiceAllocationService` 中所有 `Integer.valueOf(1).equals(user.getIsCustomerService())` 校验，统一替换为 `csConfigService.isActiveStaff(userId)`，内部实现：`SELECT 1 FROM cs_config WHERE user_id=? AND status=1`。
- 现有 `csConfigService.getOrCreateDefault(csUserId)` 隐式创建逻辑要重新评估：身份合一后，"自动 getOrCreate"会让"非客服"也意外被创建为客服记录。需改为**只读不创建**，或显式判断 status。

### 4.3 本地主库修改：`chat_visitor` 关联外部用户

```sql
ALTER TABLE chat_visitor
  ADD COLUMN external_user_id bigint DEFAULT NULL
    COMMENT '关联的外部 sys_user.id；NULL=匿名访客'
    AFTER source_page,
  ADD KEY idx_external_user (external_user_id);
```

### 4.4 本地主库修改：`cs_session` 增加来源标识 + 复合索引

```sql
ALTER TABLE cs_session
  ADD COLUMN device_type varchar(16) DEFAULT NULL
    COMMENT '来源端标识：app / miniprogram / pc / h5'
    AFTER status,
  ADD KEY idx_cs_status (cs_user_id, status);
```

### 4.5 本地主库修改：`cs_message` 增加冗余字段（用于跨 session 查历史）

```sql
ALTER TABLE cs_message
  ADD COLUMN cs_user_id bigint DEFAULT NULL AFTER session_id,
  ADD COLUMN external_user_id bigint DEFAULT NULL AFTER cs_user_id,
  ADD KEY idx_cs_external (cs_user_id, external_user_id);
```

> 写入消息时同时回填 `cs_user_id` 和 `external_user_id`，查询历史消息时直接按这两个字段聚合，无需 JOIN。

### 4.6 本地主库修改：`cs_config` 增加通知偏好 + 离线折叠天数

```sql
ALTER TABLE cs_config
  ADD COLUMN notify_enabled tinyint DEFAULT 1 COMMENT '是否启用离线通知',
  ADD COLUMN notify_min_interval_sec int DEFAULT 600 COMMENT '同一外部用户最小通知间隔（秒）',
  ADD COLUMN offline_fold_days int DEFAULT 3 COMMENT '我的客户列表中离线用户折叠到历史客户的阈值（天）';
```

---

## 五、后端实现方案

### 5.1 模块/包划分

```
custom-chat/src/main/java/com/lytboot/chat/exclusive/
├── service/
│   └── ExclusiveAllocationStrategy.java        // 专属分配判定
└── notify/
    ├── NotifyService.java                      // 接口（唯一对外入口）
    └── impl/LoggingNotifyService.java          // 实现：仅日志
```

> 客服身份相关的 domain / mapper / service 都**复用现有 `CsConfig` / `ICsConfigService`**，仅在 `ICsConfigService` 上新增 `boolean isActiveStaff(Long userId)` 方法即可。**无需新建任何身份管理 Controller**——原 `CustomerServiceController.setStaff` 的实现切换到读写 `cs_config.status` 即可。

### 5.2 访客接入流程（`/cs/connect`）

**请求格式**：
```json
POST /cs/connect
{
  "visitorToken": "xxx",
  "sourcePage": "...",
  "externalUserId": 123,
  "deviceType": "app"
}
```

**处理逻辑**：

```java
public AjaxResult connect(Map<String, String> params, HttpServletRequest request) {
    String visitorToken = params.get("visitorToken");
    if (visitorToken == null || visitorToken.isEmpty()) {
        visitorToken = UUID.randomUUID().toString().replace("-", "");
    }

    Long externalUserId = parseLong(params.get("externalUserId"));
    String deviceType = params.get("deviceType");
    String ip = getClientIp(request);
    String userAgent = request.getHeader("User-Agent");
    String sourcePage = params.get("sourcePage");

    // 1. 登录用户：查从库存在性
    if (externalUserId != null) {
        SysUser user = sysUserService.selectUserById(externalUserId);
        if (user == null) {
            return AjaxResult.error("用户不存在");
        }
    }

    // 2. 获取或创建 chat_visitor（含换人检测）
    ChatVisitor visitor = resolveVisitor(visitorToken, ip, userAgent, sourcePage, externalUserId);

    // 3. 分配
    AllocationResult result = allocationService.allocate(visitor);
    // ... 组装返回
}

/**
 * 核心安全逻辑：token + 身份一致性校验，防止同一设备换账号导致数据串号
 */
private ChatVisitor resolveVisitor(String visitorToken, String ip, String ua, String sourcePage, Long externalUserId) {
    ChatVisitor visitor = chatVisitorMapper.selectByToken(visitorToken);

    if (visitor != null && visitor.getExternalUserId() != null) {
        // 该 token 之前关联过登录用户
        if (!Objects.equals(visitor.getExternalUserId(), externalUserId)) {
            // ★ 换人了！强制生成新 token，创建全新 visitor 记录
            String newToken = UUID.randomUUID().toString().replace("-", "");
            return createVisitor(newToken, ip, ua, sourcePage, externalUserId);
        }
        // 同一人复用，更新活跃信息
        visitor.setIp(ip);
        visitor.setUserAgent(ua);
        chatVisitorMapper.updateById(visitor);
        return visitor;
    }

    if (visitor != null && visitor.getExternalUserId() == null && externalUserId != null) {
        // 匿名 token 首次绑定登录用户 → 直接升级
        visitor.setExternalUserId(externalUserId);
        chatVisitorMapper.updateById(visitor);
        return visitor;
    }

    if (visitor != null && visitor.getExternalUserId() != null && externalUserId == null) {
        // ★ token 关联过登录用户，但现在未传 externalUserId（用户退出登录）
        // 强制生成新 token，创建匿名 visitor，防止残留身份
        String newToken = UUID.randomUUID().toString().replace("-", "");
        return createVisitor(newToken, ip, ua, sourcePage, null);
    }

    if (visitor == null) {
        // 全新 token
        return createVisitor(visitorToken, ip, ua, sourcePage, externalUserId);
    }

    return visitor;
}
```

**四条安全规则**：

| 场景 | 处理 |
|---|---|
| token 关联过用户 A，现在传用户 B | **强制生成新 token**，创建新 visitor（防止串号） |
| token 关联过登录用户，现在未传 externalUserId（退出登录） | **强制生成新 token**，创建匿名 visitor（防止残留身份） |
| 匿名 token 首次传入 externalUserId | **直接升级绑定**，更新该 visitor 的 external_user_id |
| token 未使用过 | 正常创建 |

> **注意**：登录用户**不走 IP+UA 兜底**，严格按 `visitor_token` 匹配。只有匿名访客才走现有兜底逻辑。

### 5.3 分配逻辑改造点

`CustomerServiceAllocationService.allocate(visitor)` **唯一改动**——开头加专属判定短路分支：

```java
public AllocationResult allocate(ChatVisitor visitor) {
    // ★ 新增：外部用户专属判定（排在最前面）
    if (visitor.getExternalUserId() != null) {
        return exclusiveStrategy.allocate(visitor);
    }

    // 1. 已有进行中会话 → 直接返回（保留原逻辑，匿名访客用）
    CsSession active = csSessionService.selectActiveSessionByVisitorId(visitor.getId());
    if (active != null) { return ...; }

    // 2-4. 原逻辑（粘性 / 默认 / 兜底）保持不变
}
```

**`ExclusiveAllocationStrategy.allocate(visitor)` 决策树**：

```
读 sys_user.bound_cs_user_id (从库)
├─ 为空 → 返回 rejected("请联系您的业务联系人分配专属客服")
└─ 不为空 → 校验 cs_config (本地主库) where user_id=bound_cs_user_id
    ├─ 不存在 / status=0 → 返回 rejected("您的专属客服当前不可用，请联系业务联系人")
    └─ 有效
        ├─ 在线 → createSessionAndAssign(visitor, csUserId, deviceType)
        │          [session.status=1, 不写 last_cs_user_id, 不计入 activeCount]
        └─ 离线 → 创建 cs_session(status=2 等待) + setPending + addToWaiting
                  → 触发 NotifyService.notifyOffline(csUserId, visitor)
                  → 返回 waitingForLastCs(csNickname, "已发送短信通知您的专属客服 XX，请稍候")
```

> 文案"已发送短信通知"**留 TODO**：后续接入真实通知渠道后再调整为真实状态描述。

**关键差异点**：
- **不再查"是否有 active session"**——登录用户每次打开都**新建 session**（多端各自独立）
- **在线即分配，不看名额**——`max_sessions` / `activeCount` 仅用于匿名访客判断
- `createSessionAndAssign` 内部：
  - 调用 `csSessionService.createSession(visitorId, csUserId, deviceType)` 创建新 session
  - **匿名访客**：`redisManager.incrementActiveCount(csUserId)`（保持现有逻辑）
  - **登录用户**：**不调用** incrementActiveCount（不占名额）
  - `redisManager.setVisitorSession(visitorToken, sessionId, csUserId)`（与现有一致）

### 5.4 消息写入、来源标识与客服回复广播

发送消息时（`ChatChannelHandler` 或消息 Service），消息体增加 `deviceType` 字段，写入 `cs_message`：

```java
CsMessage msg = new CsMessage();
msg.setSessionId(sessionId);
msg.setCsUserId(csUserId);           // 冗余回填
msg.setExternalUserId(externalUserId); // 冗余回填
msg.setFromType(fromType);
msg.setFromUserId(fromUserId);
msg.setContent(content);
msg.setDeviceType(deviceType);        // 新增字段（device_type）
msg.setMsgType(msgType);
csMessageMapper.insert(msg);
```

**客服回复时**：`deviceType` 为空（或写 `"cs"`），表示来自客服端。

**客服回复广播**（好友式关键逻辑）：

Netty 中维护 `Map<String, Set<Channel>> userChannels`，key = `csUserId + ":" + externalUserId`：

```java
// 用户端连接时注册
public void registerUserChannel(Long csUserId, Long externalUserId, Channel channel) {
    String key = csUserId + ":" + externalUserId;
    userChannels.computeIfAbsent(key, k -> ConcurrentHashMap.newKeySet()).add(channel);
}

// 用户端断开时注销（原子操作，避免竞态）
public void unregisterUserChannel(Long csUserId, Long externalUserId, Channel channel) {
    String key = csUserId + ":" + externalUserId;
    userChannels.computeIfPresent(key, (k, channels) -> {
        channels.remove(channel);
        return channels.isEmpty() ? null : channels;
    });
}

// 客服回复时广播给该用户所有端
public void broadcastToUser(Long csUserId, Long externalUserId, Object message) {
    String key = csUserId + ":" + externalUserId;
    Set<Channel> channels = userChannels.get(key);
    if (channels != null) {
        for (Channel ch : channels) {
            if (ch.isActive()) {
                ch.writeAndFlush(message).addListener(future -> {
                    if (!future.isSuccess()) {
                        log.warn("广播消息发送失败, channel={}", ch.id());
                    }
                });
            }
        }
    }
}
```

> **注意**：用户端之间仍然不广播（A 端消息不会推给 B 端），只有**客服回复**会广播到该用户的所有在线端。`writeAndFlush` 增加 `ChannelFuture` 监听，失败时记录日志。

### 5.5 历史消息聚合查询（客服侧 + 用户侧）

```java
// 客服侧 / 用户侧查历史消息（跨 session 聚合）
GET /cs/messages/history
  ?externalUserId=123
  &csUserId=456
  &pageNum=1
  &pageSize=20

// 直接按冗余字段查，无需 JOIN
SELECT * FROM cs_message
WHERE cs_user_id = ? AND external_user_id = ?
ORDER BY create_time DESC
LIMIT 20 OFFSET ?;
```

**用户侧**：打开客服窗口即调用该 API，加载全部历史消息（分页，上滑加载更多）。
**客服侧**：点击"我的客户"条目后加载。

### 5.6 通知服务（仅留入口）

```java
public interface NotifyService {
    /**
     * 通知专属客服有访客在等待。
     * 当前仅日志打印；后续确定通知方式（短信/邮件/企微）后由实现类替换。
     */
    void notifyOffline(Long csUserId, ChatVisitor visitor, String reason);
}

@Service
@Slf4j
public class LoggingNotifyService implements NotifyService {
    @Resource private ICsConfigService csConfigService;
    @Resource private RedisCache redisCache;

    @Override
    @Async
    public void notifyOffline(Long csUserId, ChatVisitor visitor, String reason) {
        // 1. 读配置（仅读，不创建；外层已校验 csUserId 是有效客服）
        CsConfig cfg = csConfigService.getByUserId(csUserId);
        if (cfg == null || Integer.valueOf(0).equals(cfg.getNotifyEnabled())) return;

        // 2. 限流：cs:notify:rate:{csUserId}:{externalUserId}
        String key = "cs:notify:rate:" + csUserId + ":" + visitor.getExternalUserId();
        Boolean ok = redisCache.setIfAbsent(key, "1", cfg.getNotifyMinIntervalSec(), TimeUnit.SECONDS);
        if (!Boolean.TRUE.equals(ok)) return;

        // 3. 当前仅打印日志
        log.info("[NotifyService] csUserId={} externalUserId={} visitorId={} reason={} | 留待短信/邮件/企微接入",
                 csUserId, visitor.getExternalUserId(), visitor.getId(), reason);
    }
}
```

**触发点**：仅在 `ExclusiveAllocationStrategy.allocate()` 中"离线"分支调用一次即可。**不**重复在 `ChatChannelHandler` 收消息时再次触发。

### 5.7 客服身份管理接口（改造现有，无新接口）

保留现有 `CustomerServiceController.setStaff` 路径，仅改写实现：

| 操作 | 现有实现 | 改造后实现 |
|---|---|---|
| 设为客服 | `UPDATE sys_user SET is_customer_service=1` | `INSERT INTO cs_config(user_id, max_sessions, status) VALUES(?, 5, 1) ON DUPLICATE KEY UPDATE status=1` |
| 取消客服 | `UPDATE sys_user SET is_customer_service=0` | `UPDATE cs_config SET status=0 WHERE user_id=?`（保留配置数据，仅切状态） |
| 客服列表 | 查 `sys_user WHERE is_customer_service=1` | 查 `cs_config c JOIN sys_user u ON c.user_id=u.user_id WHERE c.status=1` |

**注意**：`CustomerServiceAllocationService` 内所有 `Integer.valueOf(1).equals(user.getIsCustomerService())` 校验需替换为 `csConfigService.isActiveStaff(userId)`，内部 `SELECT 1 FROM cs_config WHERE user_id=? AND status=1`。

`ICsConfigService` 上新增以下方法：
- `boolean isActiveStaff(Long userId)`
- `CsConfig getByUserId(Long userId)`（仅读，不再隐式创建——替代或谨慎使用现有 `getOrCreateDefault`）

---

## 六、前端改造点

### 6.1 访客 SDK / 用户端

- 初始化时支持 `externalUserId` 配置项（从外部系统页面注入）；不传 = 匿名访客模式（与现有完全兼容）。
- 初始化时支持 `deviceType` 配置项（默认 `"h5"`）。
- `/cs/connect` 返回字段处理：

| 后端返回 | 前端行为 |
|---|---|
| `success: true` | 进入聊天界面，**加载全部历史消息**（分页上滑） |
| `waiting: true` | 显示"当前咨询繁忙，请稍候"（现有逻辑，匿名访客用） |
| `waitingForLastCs: true` | 显示后端 `message`（如"已发送短信通知您的专属客服 XX，请稍候"），允许继续输入消息（进入留言态） |
| `rejected: true` | 弹窗显示后端 `message`，**阻止建立 WebSocket** |

- **用户侧打开即加载历史消息**：接入时调用 `/cs/messages/history` API，渲染历史聊天记录，底部正常输入。

### 6.2 客服工作台 `views/cs/workbench/index.vue`

**左侧列表分两个分组**：

```
┌─────────────────────────────┐
│ 📌 我的客户（在线/今日活跃）  │
│   张三 [在线] 📱    2分钟前   │
│   李四 [在线] 💻    15分钟前  │
│                             │
│ 📂 历史客户（离线超7天）      │
│   王五            3天前      │
│   赵六            10天前     │
│                             │
│ 💬 临时会话                  │
│   访客#1234         刚刚     │
│   访客#5678         5分钟前  │
└─────────────────────────────┘
```

**📌 我的客户（登录用户）**：
- 数据来源：`GET /cs/workbench/customers`（按 `bound_cs_user_id = 当前客服ID` 从从库查，聚合该用户最后消息时间、未读数、在线状态）
- 每个条目是一个**用户**（external_user_id 维度），不是 session
- 在线状态：该用户是否有任意端建立了 WebSocket 连接
- **默认只显示"在线"或"今日有消息"的用户**；离线超过 3 天的用户自动折叠到"📂 历史客户"子分组（可展开）。阈值读取 `cs_config.offline_fold_days`，客服可在"人员设置"中自行调整
- 提供搜索框按用户名/ID 过滤
- 按 `last_msg_time` 倒序排列
- 点击打开右侧聊天面板：加载该用户**全部历史消息**（跨 session，分页上滑加载）
- 每条消息旁显示 `deviceType` 小标签（📱APP / 💻PC / 小程序）
- 底部输入框回复 → 客服回复广播到该用户所有在线端

**💬 临时会话（匿名访客）**：
- 数据来源：现有进行中/等待中的 `cs_session`
- 每个条目是一个 session
- session 结束后（status=0）自动从列表消失
- 按现有逻辑处理

**右侧聊天面板通用**：
- 转接按钮对专属会话隐藏或置灰（"专属会话不可转接"）
- 输入框上方显示当前用户在线端标识（如"用户在线：📱APP 💻PC"）

### 6.3 客服人员设置 `views/cs/staff/index.vue`

- 列表数据源从 `sys_user.is_customer_service=1` 改为 `cs_config WHERE status=1`。
- 增加"是否启用离线通知"开关（写 `cs_config.notify_enabled`）。
- 增加"通知间隔（秒）"输入（写 `cs_config.notify_min_interval_sec`）。
- 增加"离线用户折叠天数"输入（写 `cs_config.offline_fold_days`，默认 3 天，控制工作台左侧"历史客户"折叠阈值）。
- **`max_sessions` 配置项旁增加说明**："仅约束匿名访客数量，登录用户不占名额"。

### 6.4 不新增前端页面

绑定关系完全在外部系统侧维护，客服系统**不**提供"专属客户管理"页面。

---

## 七、与现有功能的兼容性矩阵

| 现有功能 | 影响 | 兼容方案 |
|---|---|---|
| 匿名访客 `/cs/connect` | 无 | `externalUserId` 未传 → 走匿名分支，原逻辑零改动 |
| 粘性分配 `last_cs_user_id` | 无 | 专属分支不写 `last_cs_user_id` |
| 会话转接 `/cs/transfer` | **限制** | 接收前校验 session 关联的 visitor.external_user_id；非空 → 拒绝转接（"专属会话不可转接"） |
| 客服下线关闭会话 | 部分调整 | 专属客户的 session 不重新分配，挂起 + 通知（与离线一致） |
| 等待队列 `cs:waiting` | 无 | 专属访客同样进队列，`processPendingOnCsOnline` 优先消费 |
| `setStaff` / `is_customer_service` | **改造** | 切换到 `cs_config.status`，一次性数据迁移补齐缺失记录 |
| Netty WebSocket 推送 | **改造** | 新增 `externalUserId → Set<Channel>` 映射，客服回复广播到该用户所有端 |
| `csConfigService.getOrCreateDefault` | **改造** | 第 1 期改造为只读或加 status 校验，避免非客服意外创建记录 |

---

## 八、实施步骤

### 第 1 期：客服身份合并到 `cs_config`（0.3 周）
- 数据迁移脚本：把 `sys_user.is_customer_service=1` 但 `cs_config` 中缺失的记录批量补齐
- `ICsConfigService` 新增 `isActiveStaff(userId)` 和 `getByUserId(userId)`
- `CustomerServiceController.setStaff` 改为读写 `cs_config.status`
- `CustomerServiceAllocationService` 内所有 `is_customer_service` 校验改为 `csConfigService.isActiveStaff(userId)`
- **全局搜索替换**：确保 WebSocket 认证（`WebSocketHandler.handleAuthMessage`）等所有引用点都已替换
- 评估并改造 `csConfigService.getOrCreateDefault` 的隐式创建行为，避免污染身份判定
- 验证：现有客服功能（上线、分配、转接）回归通过

### 第 2 期：Schema 变更 + 访客接入改造（0.2 周）
- 执行 DDL：`chat_visitor.external_user_id`、`cs_session.device_type`、`cs_message.cs_user_id` / `external_user_id`、`cs_config.notify_enabled` / `notify_min_interval_sec`、`cs_session` 复合索引 `idx_cs_status`
- `/cs/connect` 接受 `externalUserId` + `deviceType` 入参
- 改造 `getOrCreateVisitor` / `resolveVisitor`：登录用户**不走 IP+UA 兜底**，严格按 `visitor_token` 匹配，含四条换人检测规则
- 回填 `chat_visitor.external_user_id`

### 第 3 期：专属分配逻辑 + 挂起/心跳修复（0.3 周）
- `AllocationResult.rejected()` 工厂 + 字段透传
- `ExclusiveAllocationStrategy` 实现（含完整决策树：在线直接分配、离线挂起等待）
- `allocate()` 注入专属短路分支
- **转接接口加专属会话拦截**
- **`processPendingOnCsOnline` 加分布式锁**（复用 `cs:allocate:lock:{csUserId}`）
- **`processPendingOnCsOnline` 专属访客跳过 `activeCount` 容量检查**
- **客服心跳超时处理（`CsHeartbeatCheckTask.handleCsTimeout`）区分专属会话**：专属会话不 `tryConsumeWaitingAfterClose`，而是 `setPending` + `notifyOffline`
- 验证：登录用户走专属、匿名走原逻辑

### 第 4 期：Netty 广播映射 + 消息回填（0.4 周）
- Netty 新增 `userChannels` 映射（`csUserId:externalUserId → Set<Channel>`）
- **用户端连接/断开时注册/注销 channel**（`unregisterUserChannel` 使用 `computeIfPresent` 原子操作）
- 客服回复时遍历 `userChannels` 广播到该用户所有在线端（含 `ChannelFuture` 失败监听）
- **`ChatChannelHandler` 和 `ConnectionManager` 梳理**：明确广播逻辑注入到哪套 Handler，避免双轨遗漏
- `ChatChannelHandler` 消息体解析 `deviceType`，写入 `cs_message`
- **客服回复时 `deviceType` 标记为 `"cs"`**
- **`CsMessageService.sendMessage` 增加冗余字段回填**（内部通过 `sessionId` 查 `cs_user_id` / `external_user_id` 回填）
- 新增 `/cs/messages/history` 聚合查询 API（按 `cs_user_id + external_user_id`）

### 第 5 期：好友式分组 UI + 前端适配 + 清理任务（0.5 周）
- 新增 `GET /cs/workbench/customers` API（按 bound_cs_user_id 聚合查询，含最后消息时间、未读数、在线状态）
- 客服工作台左侧分"📌 我的客户" / "📂 历史客户" / "💬 临时会话"三组
- 我的客户列表：默认只显示在线/今日活跃，离线超 3 天折叠（阈值读取 `cs_config.offline_fold_days`，支持在人员设置中动态调整），支持搜索
- 输入框上方显示用户在线端标识
- 消息旁显示 `deviceType` 标签
- **`max_sessions` 配置项增加说明"仅约束匿名访客"**
- **用户侧打开即加载历史消息**（前端调用 `/cs/messages/history`）
- `NotifyService` 接口 + `LoggingNotifyService` 实现
- 触发点接入 `ExclusiveAllocationStrategy`（离线分支）
- 限流键 + Redis SETNX
- 访客 SDK：`externalUserId` + `deviceType` 透传
- `/cs/connect` 返回值新增 `rejected` / `waitingForLastCs` 文案处理
- ~~加定时任务清理 `status=0` 且结束时间超过 90 天的 `cs_session` 记录~~（**暂不实施**，后续视表增长情况再定）

**总计：约 1.7 周**（不含与外部系统的对接联调）。

---

## 九、风险与边界

| 风险 | 缓解 |
|---|---|
| `cs_config` 与从库 `sys_user` 不一致（用户已删除但 cs_config 仍存在） | 客服上线时校验 `sys_user` 是否存在；定期清理任务（可选） |
| 专属客服永久离职导致访客永远等不到 | 业务方需在外部系统侧重新绑定；客服系统侧给"专属失效拒绝服务"明确文案 |
| 并发：访客 setPending 时客服恰好上线 | 复用 `cs:allocate:lock:{csUserId}` 分布式锁 |
| 多数据源改造未完成时本方案能否独立实施 | **可以**——`cs_config` 在主库；`bound_cs_user_id` 暂时也可在主库 sys_user 上加，多数据源完成后字段位置不变 |
| `cs_config.status` 替换 `is_customer_service` 涉及多处 if 改写 | 提供 `csConfigService.isActiveStaff(userId)` 单一封装，替换工作量收敛在 `CustomerServiceAllocationService` 一个文件 |
| `csConfigService.getOrCreateDefault` 隐式创建会让"非客服"也意外有 cs_config 记录 | 第 1 期中显式改造为只读或加 status 校验，避免身份污染 |
| 登录用户每次打开新建 session 导致表膨胀 | `cs_session` 增加快，但单条记录极小；**暂不实施定时清理**，后续视表增长情况再决策 |
| 通知当前仅打印日志 | **留 TODO**：后续确定通知方式（短信/邮件/企微）后实装 |
| Redis 限流键丢失导致通知风暴 | 当前通知仅日志打印，无真实费用；Redis 重启是小概率事件；后续接入真实通知时考虑 MySQL 持久化限流状态 |
| 客服回复广播时某端断开 | `ChannelFuture` 监听记录日志；用户侧加载历史消息后可缓解 |

---

## 十、外部系统侧需要配合的事项

> 客服系统不能独立完成的部分，需要外部业务系统侧承诺：

1. ✅ `sys_user` 表加 `bound_cs_user_id` 字段（schema 变更 + 写入逻辑）
2. ✅ 用户登录后，将 `userId` 注入页面上下文（如后端渲染到 HTML 的 JS 变量），客服 SDK 直接透传
3. ✅ 提供"换绑专属客服"的管理界面（客服系统不再提供）
4. ✅ 前端在初始化客服 SDK 时传入 `deviceType`（app / miniprogram / pc / h5）
5. ⚠️ 同步通知客服系统的 DBA：从库 schema 变更需在客服系统读取前完成

---

## 十一、待确认问题（已决策）

以下问题已由用户确认最终决策，文档已同步更新：

| # | 问题 | 最终决策 |
|---|------|----------|
| 1 | **session 清理周期** | **暂不实施**。后续视 `cs_session` 表增长情况再决定是否添加定时清理任务 |
| 2 | **通知渠道优先级** | **暂不决定**。当前仅留 `NotifyService` 接口 + 日志实现，后续业务方确定渠道（短信/邮件/企微/钉钉）后再实装 |
| 3 | **离线用户折叠阈值** | **3 天**。已在 `cs_config` 新增 `offline_fold_days` 字段（默认 3），客服可在"人员设置"中自行调整 |
| 4 | **专属客服离职后的批量换绑** | **由外部系统侧实现**。客服系统不提供批量换绑 API，仅提供"专属客服失效 → 拒绝服务"的兜底提示 |

---

如无异议，按第 1～5 期顺序实施。
